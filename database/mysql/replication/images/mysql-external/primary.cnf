# Mirror of ../primary.cnf, baked into image to avoid Windows bind-mount permission issues.

[mysqld]
server-id=1
log_bin=mysql-bin
binlog_format=ROW
gtid_mode=ON
enforce_gtid_consistency=ON
log_slave_updates=ON
read_only=OFF
skip_name_resolve=1
