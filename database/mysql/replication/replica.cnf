[mysqld]
server-id=2
relay_log=relay-bin
read_only=ON
super_read_only=ON

log_bin=mysql-bin
binlog_format=ROW
binlog_row_image=FULL
gtid_mode=ON
enforce_gtid_consistency=ON
log_replica_updates=ON
skip_replica_start=ON

skip_name_resolve=1
